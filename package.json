{
  "name": "bazaaragent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "check": "bunx tsc --noEmit",
    "test": "bun test",
    "bazaar": "bun run packages/agent/src/cli.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "latest"
  }
}
